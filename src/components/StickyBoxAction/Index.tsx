import { light } from '@/src/theme';
import { StickyBoxStyle } from './StickyBoxAction.styled';

const StickyBoxAction = () => {
	return <StickyBoxStyle>Add to collection</StickyBoxStyle>;
};

export default StickyBoxAction;

import styled from '@emotion/styled';

export const Input = styled.input`
	width: 100%;
	padding: 0.8rem 1rem;
	font-family: inherit;
	border: 1px solid #b0adad;
	border-radius: 8px;
	outline: none;

	&::placeholder {
		color: #b0adad;
	}
`;
